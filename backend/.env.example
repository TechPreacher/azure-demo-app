# Backend Environment Configuration
# Copy this file to .env and fill in your values

# Storage Configuration
# Use "local" for local development, "azure" for Azure Blob Storage
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=../data/services.json

# Azure Storage (when STORAGE_TYPE=azure)
# AZURE_STORAGE_ACCOUNT_NAME=your-storage-account
# AZURE_BLOB_CONTAINER_NAME=data
# AZURE_STORAGE_USE_MANAGED_IDENTITY=true

# Telemetry Configuration (Azure Monitor OpenTelemetry)
# Required for Application Insights integration
# Leave empty to disable telemetry (graceful degradation)
APPLICATIONINSIGHTS_CONNECTION_STRING=
OTEL_SERVICE_NAME=azure-service-catalog-backend
